using BT;
public class PlayerBT : BTTree
{
    protected override BTNode SetupTree()
    {
        {
            // 创建节点
            Sequence root = new Sequence();
            return root;
        }
    }
}